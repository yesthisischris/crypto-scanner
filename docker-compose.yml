services:
  simple-trader:
    build: .
    ports:
      - "8787:8787"
    environment:
      - TAAPI_KEY=${TAAPI_KEY}
      - CMC_KEY=${CMC_KEY}
      - OPENAI_API_KEY=${OPENAI_API_KEY}
      - MCP_HTTP=1
      - PORT=8787 
    volumes:
      - .env:/app/.env:ro
